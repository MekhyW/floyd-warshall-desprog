import StateInline from 'markdown-it/lib/rules_inline/state_inline';
/**
 * If successful, returns end pos.
 * Else, returns -1
 */
export declare const parseColorName: (state: StateInline, start: number, disableNested: boolean) => number;
